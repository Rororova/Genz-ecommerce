# Supabase Configuration
# Get these from your Supabase project dashboard: https://app.supabase.com

# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase SERVICE ROLE key (not the anon key!)
# ⚠️ IMPORTANT: Use the service role key for server-side operations
# This key bypasses Row Level Security and should NEVER be exposed client-side
# Keep this secret and only use it in your backend/server code
SUPABASE_KEY=your-service-role-key-here

# Optional: MongoDB URI (if you want to use MongoDB instead)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# Port for Express server (optional, defaults to 3000)
# PORT=3000
